<%= form_for(@serie) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Crear nueva serie..." %>
  </div>
  <%= f.submit "Serie", class: "btn btn-primary" %>
<% end %>
