<% provide(:title, @user.name) %>
<div class="jumbotron">
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h4><font color="black">
        <%= gravatar_for @user %><br>
        <%= @user.name %><br><br><br><br><br>
        <ul class="nav nav-pills nav-stacked">
          <li class="active"><%= link_to 'Editar cuenta', {:controller => :users, :action => :edit}%></li>
          <li class="active"><%= link_to 'Administrar géneros de series', {:controller => :genders, :action => :index} %></li>
          <li class="active"><%= link_to 'Administrar cuentas niño',boy_accounts_path %></li>
        </ul>
      </font></h4>
    </section>
  </aside>
  <div class="col-md-8">
     <% if @user.movies.any? %>
      <h3>Series agregadas por <%= @user.name %> <span class="badge"><%= @user.movies.count %></span></h3>
      <ol class="movies">
        <%= render @movies %>
      </ol>
    <% end %>
  </div>
  </div>
</div>
